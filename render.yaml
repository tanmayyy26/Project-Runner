services:
  - type: web
    name: github-project-runner
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
      - key: DOCKER_ENABLED
        value: "false"
